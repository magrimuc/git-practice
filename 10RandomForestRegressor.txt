RandomForestRegressor
 # Define parameter distribution for sampling
param_dist = {    'n_estimators': [80,90,100, 200, 300, 500],      'max_depth': [None, 10, 20, 30],         'min_samples_split': [2, 5, 10],         'min_samples_leaf': [1, 2, 4],    'max_features': ['sqrt', 'log2', None] 
#number of trees, # tree depth, # min samples per leaf,  # min samples to split, # number of features per split }
# Randomized search
random_search = RandomizedSearchCV(    estimator=rf,    param_distributions=param_dist,    n_iter=200,  cv=3,  scoring='neg_mean_squared_error',    n_jobs=-1,     verbose=2,    random_state=42) # fit # number of random combinations to try HORST spart Zeit
print("Best parameters:", random_search.best_params_)
best_rf = random_search.best_estimator_
#bagging (and boosting) is considering overfitting.. (as it is done here)
#also: Reducing the number of boosting iterations, using regularization, or adjusting learning rate
#HistGradientBoostingRegressor and XGBRegressor proven to be very good models

