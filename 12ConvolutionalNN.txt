logistic regression (classification alg) - predicts prob. 4 One class
https://de.wikipedia.org/wiki/Logistische_Regression
Sigmoid funct (nicht die einzige wi + w_b) #w_b eigener _Bias ... für binär Entschiedungen outputl
SOLL: nicht linear f(x) + f(z) != f(x+z)
ReLU - RectifiedLinearUnit = max(0,x) # auch n lin; hiddenlayer fav.
softmax - multiclass class input vec_i... output vec_j; immer e^(z_i) / sum e^(z_j); ## j<= #cl; when >2 cl
from keras.optimizers import Adam
model = Sequential( ) #.add ; .compile ; .fit(xy_train, validation_data=(x_test, y_test); # Dropout - Pause gut
hierarch feat: low-lev edges2high-lev shapes
conv layers: small (2-5x2-5) learnable filters/kernels /{dot product} input image; "output": feature map
 Conv2D(32, (3,3), activation='relu', input_shape=(28,28,1)),
pooling: aggregate sum,max however 4->1 e.g. 
 MaxPooling2D(2,2), # stride: Schrittgröße
flattening: 1 dim; 
 Flatten(),
fully connected or dense layer (sigmoid binary, softmax > 2 cl)
 Dense(128, activation='relu'),
 Dense(10, activation='softmax') # dim == y_labels